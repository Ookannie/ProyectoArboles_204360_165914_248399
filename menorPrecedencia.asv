function menor = menorPrecedencia(operador1, operador2)
    menor = 0;
    if(((operador2 ~= '(') || (operador2 ~= '[') || (operador2 ~= '{')) && 
        ((operador2 ~= ')') || (operador2 ~= '}') || (lista{i} ~=  ']')))
        menor = getPrecedence(operador1) <= getPrecedence(operador2);
    end
end